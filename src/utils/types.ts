export interface LayoutProps {
	title: string;
}

export interface Property {
	title: string;
	description: string;
	buttonText: string;
	images: string[];
}

export interface ImageProps {
	src: string;
	alt: string;
}
